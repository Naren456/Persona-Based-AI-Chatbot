# Frontend

Expo Router frontend for the Persona-Based AI Chatbot.

## Run

```bash
npm install
npm run web
```

Set `EXPO_PUBLIC_API_URL` if your backend is not running at `http://localhost:4000`.

```bash
EXPO_PUBLIC_API_URL=https://your-backend.example.com npm run web
```
